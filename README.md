# DailyStandupList---Agile-Tool

heres the current version of the code:
  1) names are saved in local storage when "generate list" button is pressed. can randomize the list to shuffle order of names
  2) note containers are displayed for each name 
  3) notes are automatically saved in loacal storage when typed (no need to press button).
  4) button for downlaoding notes
  5) when page is reloaded, names and notes are parsed from local storage.
  6) can still edit the notes and it will be saved automatically(no need to press button).
